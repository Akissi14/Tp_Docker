---
# tasks file for roles/proxy
- name: Run HTTPD Proxy
  docker_container:
    name: my-httpd
    image: akissi14/tp-devops-httpd:latest  
    networks:
      - name: app-network
    ports:
      - "80:80"
    restart_policy: always